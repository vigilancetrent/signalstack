# signalstack runs as a self-contained demo with synthetic data.
# No environment variables are required for the default experience.
#
# Optional knobs (read in lib/syntheticData.ts via process.env if you
# choose to wire them up):
#
# SIGNALSTACK_SEED=42
# SIGNALSTACK_TICK_MS=200
